name: Deploy to GitHub Pages

on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pages: write
      id-token: write
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - uses: actions/checkout@v4
      - uses: oven-sh/setup-bun@v2
      - run: bun install
      - run: bun run build
        env:
          VITE_SUPABASE_URL: ${{ secrets.VITE_SUPABASE_URL }}
          VITE_SUPABASE_PUBLISHABLE_KEY: ${{ secrets.VITE_SUPABASE_PUBLISHABLE_KEY }}
      - name: Create index.html
        run: |
          CSS=$(ls dist/client/assets/*.css | xargs -I{} basename {})
          JS=$(ls dist/client/assets/index-*.js | tail -1 | xargs -I{} basename {})
          cat > dist/client/index.html << EOF
          <!DOCTYPE html>
          <html lang="ja">
          <head>
            <meta charset="utf-8" />
            <meta name="viewport" content="width=device-width, initial-scale=1" />
            <title>KAZUTCHI — Dancer & Choreographer</title>
            <base href="/artist-folio-prime/" />
            <link rel="stylesheet" href="/artist-folio-prime/assets/$CSS" />
          </head>
          <body>
            <div id="root"></div>
            <script type="module" src="/artist-folio-prime/assets/$JS"></script>
          </body>
          </html>
          EOF
          cp dist/client/index.html dist/client/404.html
      - uses: actions/upload-pages-artifact@v3
        with:
          path: dist/client
      - uses: actions/deploy-pages@v4
